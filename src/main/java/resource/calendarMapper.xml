<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="sopms.calendar.dao.calendarDao">
	<resultMap type="cal" id="calResult">
		<result property="start" column="start1"/>
		<result property="end" column="end1"/>
	</resultMap>
	<resultMap type="cal" id="calResult2">
		<result property="start" column="start_date"/>
		<result property="end" column="end_date"/>
	</resultMap>
	<select id="calList" resultMap="calResult" parameterType="cal">
		SELECT a.*, b.name
		FROM calendar a, MEMBER b
		WHERE a.MANAGER = b.id
		AND a.MANAGER = #{manager}
	</select>
	<insert id="insertCalendar" parameterType="cal">
		insert into calendar values(cal_seq.nextval, #{workcode}, #{title}, #{content},
		#{start}, #{end}, #{allDay}, #{borderColor}, #{backgroundColor}, #{textColor}, #{manager}, #{process})
	</insert>
	<update id="updateCalendar" parameterType="cal">
		UPDATE calendar
				SET title = #{title},
					content = #{content},
					start1 = #{start},
					end1 = #{end},
					allday = #{allDay},
					bordercolor = #{borderColor},
					backgroundcolor = #{backgroundColor},
					textcolor = #{textColor},
					process = #{process}
		WHERE id = #{id}
	</update>
	<delete id="deleteCalendar" parameterType="int">
		delete from calendar where id = #{id}
	</delete>
</mapper>